<p id="notice"><%= notice %></p>

<h1>All Courses</h1>
          <div class="row">
 <% @courses.each do |course| %>
                     <div class="col-xs-6 col-lg-4">     
      <h1>
        <%= course.title %>
      </h1>  
      <h1>
        <%= link_to image_tag(course.img ,  size: "100x100") ,course  %>
      </h1>
      
                     </div>
    <% end %>
          </div>
<br>