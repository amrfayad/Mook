<%= form_for(@lecture) do |f| %>
  <% if @lecture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lecture.errors.count, "error") %> prohibited this lecture from being saved:</h2>

      <ul>
      <% @lecture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <%= f.label :content %><br>
    <%= f.cktext_area :content %>
  </div>
  
  <div class="form-group">
    <%= f.label :attachfile %><br>
    <%= f.file_field :attachfile %>
  </div>
  
  <div class="form-group">
    <%= f.label :course_id %><br>
    <%= f.text_field :course_id %>
  </div>
  
    <%= f.submit "Create New Lecture"  ,class: 'btn btn-info' %>
  
<% end %>
